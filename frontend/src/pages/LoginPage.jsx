import LoginForm from '../components/Auth/LoginForm';

const LoginPage = () => {
    return (
        <div className="auth-container">
            <LoginForm />
        </div>
    );
};

export default LoginPage;
